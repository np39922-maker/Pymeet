POSTGRES_USER=pymeet
POSTGRES_PASSWORD=replace-with-a-long-random-password
POSTGRES_DB=pymeet
JWT_SECRET_KEY=replace-with-at-least-32-random-characters
PASSWORD_RESET_KEY=replace-with-a-private-recovery-key
FRONTEND_ORIGIN=http://localhost:5173
APP_PORT=5173
BIND_ADDRESS=127.0.0.1
LAN_IP=192.168.1.10
HTTPS_PORT=5443

# Live server only (no http:// or path):
DOMAIN=meet.example.com

# Recommended for reliable calls across restrictive networks (optional locally):
VITE_TURN_URL=
VITE_TURN_USERNAME=
VITE_TURN_CREDENTIAL=
